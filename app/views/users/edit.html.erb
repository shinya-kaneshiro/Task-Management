<% provide(:title, "#{current_user.name}") %>
<% provide(:class_text, 'edit') %>
<% provide(:button_text, '更新') %>
<% provide(:button_size, 'block') %>
<h1>ユーザー情報の更新</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>